# AIEmergencyTools 环境变量配置示例
# 复制此文件为 .env 并填入您的实际配置
# 警告：不要将 .env 文件提交到版本控制系统！

# ============================================
# VirusTotal API 配置（可选）
# ============================================
# 获取 API Key: https://www.virustotal.com/
# 格式: base64编码的 "api-key+username"
# 生成命令: echo -n "your-api-key+your-username" | base64
VIRUSTOTAL_API_KEY=your-base64-encoded-api-key-here

# ============================================
# SSH 连接默认配置（可选）
# ============================================
DEFAULT_SSH_PORT=22
DEFAULT_SSH_USERNAME=emergency_user
# DEFAULT_SSH_PASSWORD=  # 不建议在文件中存储密码

# ============================================
# Shellcode 分析配置（可选）
# ============================================
# 反汇编深度（默认显示前 20 条指令）
DISASM_DEPTH=20

# 字符串最小长度（默认 4 字符）
MIN_STRING_LENGTH=4

# 启用详细输出（0 或 1）
VERBOSE=0
